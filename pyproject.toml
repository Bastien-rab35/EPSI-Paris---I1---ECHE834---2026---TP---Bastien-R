[project]
name = "lego-manager"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.2",
    "requests>=2.32.5",
    "sqlalchemy>=2.0.48",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
